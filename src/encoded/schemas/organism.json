{
    "title": "Organism",
    "id": "/profiles/organism.json",
    "$schema": "http://json-schema.org/draft-04/schema#",
    "type": "object",
    "required": ["name", "taxon_id"],
    "identifyingProperties": ["uuid", "name", "taxon_id", "scientific_name"],
    "additionalProperties": false,
    "mixinProperties": [
        { "$ref": "mixins.json#/schema_version" },
        { "$ref": "mixins.json#/uuid" }
    ],
    "properties": {
        "schema_version": {
            "default": "1"
        },
        "name": {
            "title": "Common name",
            "description": "A short unique name for the strain (e.g. 'S288C' or 'W303').",
            "type": "string"
        },
        "scientific_name": {
            "title": "Binomial name",
            "description": "The genus species for the organism (e.g. 'Mus musculus').",
            "type": "string",
            "default": ""
        },
          "taxon_id": {
            "title": "Taxon ID",
            "description": "The NCBI taxon ID for the strain (e.g. 10090).",
            "type": "string",
            "format": "^[0-9]+$"
        }
    }
}
